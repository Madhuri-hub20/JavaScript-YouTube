//scope

if(true){
    const username = "Madhuri"
    if(username === "Madhuri"){
        const surname =" Navale"
        console.log(username  + surname) //utput : Madhuri Navale
    }
    //console.log(surname);if we declare like this getting eror boz we use outside the scope
}
//console.log(username);//if we declare like this getting eror boz we use outside the scope